<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <springProperty scope="context" name="serviceName" source="spring.application.name" defaultValue="cognito-rbac"/>
    <springProperty scope="context" name="environment" source="deployment.environment" defaultValue="local"/>
    <springProperty scope="context" name="applicationLogLevel"
                    source="logging.level.com.designpattern.cognitorbac" defaultValue="INFO"/>

    <!--
        LOCAL profile: human-readable coloured output for development.
        This is also the default profile configured in application.yaml.
    -->
    <springProfile name="local">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} %highlight(%-5level) [%thread] %cyan(%logger{36}) - %msg%n</pattern>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!--
        Default (all non-local environments): structured JSON output consumed by CloudWatch.
        Active whenever SPRING_PROFILES_ACTIVE does not include 'local'. Deployments
        must explicitly activate a profile such as dev, staging, or production.

        Each log line is a single-line JSON object. CloudWatch Logs Insights
        can query any field directly — e.g.:
            fields @timestamp, level, event, requestId, userSub, errorCode, auditAction
            | filter level = "ERROR"
            | sort @timestamp desc
    -->
    <springProfile name="!local">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <!-- Standard fields included in every log line -->
                <fieldNames>
                    <timestamp>timestamp</timestamp>
                    <message>message</message>
                    <logger>logger</logger>
                    <thread>thread</thread>
                    <level>level</level>
                    <levelValue>[ignore]</levelValue>
                    <version>[ignore]</version>
                </fieldNames>

                <timestampPattern>yyyy-MM-dd'T'HH:mm:ss.SSSXXX</timestampPattern>
                <timeZone>UTC</timeZone>

                <!-- App-level fields on every line -->
                <customFields>{"schemaVersion":"1","service":"${serviceName}","environment":"${environment}"}</customFields>

                <!-- MDC fields are promoted to top-level JSON fields automatically -->
                <includeContext>false</includeContext>
                <includeMdc>true</includeMdc>

                <!-- SLF4J key-value pairs (log.atInfo().addKeyValue(...)) promoted too -->
                <includeKeyValuePairs>true</includeKeyValuePairs>

                <!-- Stack traces as a JSON array, not a multi-line string -->
                <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
                    <maxDepthPerThrowable>20</maxDepthPerThrowable>
                    <shortenedClassNameLength>40</shortenedClassNameLength>
                    <rootCauseFirst>true</rootCauseFirst>
                </throwableConverter>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!-- Package-level overrides apply in both profiles -->
    <logger name="com.designpattern.cognitorbac" level="${applicationLogLevel}"/>
    <logger name="software.amazon.awssdk" level="WARN"/>
    <logger name="org.springframework.data.mongodb" level="WARN"/>

</configuration>
